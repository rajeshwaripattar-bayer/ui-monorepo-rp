import { render } from '@testing-library/react'

import QuoteModalContainer from './QuoteModalContainer'

describe('QuoteModalContainer', () => {
  it('should render successfully', () => {
    // const { baseElement } = render(<QuoteModalContainer />)
    // expect(baseElement).toBeTruthy()
  })
})
