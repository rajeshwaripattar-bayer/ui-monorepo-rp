import { render } from '@testing-library/react'

import SelectStorageLocation from './SelectStorageLocation'

describe('SelectStorageLocation', () => {
  it('should render successfully', () => {
    // const { baseElement } = render(<SelectStorageLocation />)
    // expect(baseElement).toBeTruthy()
  })
})
