import { render } from '@testing-library/react'

import DiscountListByEntry from './DiscountListByEntry'

describe('DiscountListByEntry', () => {
  it('should render successfully', () => {
    // const { baseElement } = render(<DiscountListByEntry />)
    // expect(baseElement).toBeTruthy()
  })
})
